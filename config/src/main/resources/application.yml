server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/EFFM00/C10-Taller-Design-Patterns
          default-label: master
          force-pull: true
          search-paths: config-data-repo